server:
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: device-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:device_db}
    username: ${DB_USERNAME:iot_user}
    password: ${DB_PASSWORD:iot_pass}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

app:
  mqtt:
    broker-url: ${MQTT_BROKER_URL:tcp://broker.hivemq.com:1883}
    client-id: ${MQTT_CLIENT_ID:iot-device-service}
    qos: ${MQTT_QOS:1}
    keep-alive-interval: ${MQTT_KEEP_ALIVE:60}
    connection-timeout: ${MQTT_CONN_TIMEOUT:30}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true
